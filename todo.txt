└── use map and csv parser to parse first line and get indexes of the fields to then use onto the assigners
    ├── you have all the fields for stops
    ├── you need sort_order for routes
    └── first #include <map> then std::map<string, int> ohio; then ohio["yes"] = 67; 